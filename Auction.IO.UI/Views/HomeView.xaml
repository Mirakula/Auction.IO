<UserControl x:Class="Auction.IO.UI.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Auction.IO.UI.Views"
             xmlns:controls="clr-namespace:Auction.IO.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Home view"/>

        <StackPanel Grid.Row="1" Margin="20">
            <controls:ItemCard  Margin="0, 10" DataContext="{Binding ItemViewModel}" HorizontalAlignment="Left" />
        </StackPanel>
    </Grid>
</UserControl>
